package stdlib

# Parent class for all other classes in the Silicon programming language.
public class Object {

    # Prints an object to the console; it will be converted to a string
    # representation in native code.
    native func print(msg : Object)

    # Returns a string representation of this object.
    native func toString() -> String

}